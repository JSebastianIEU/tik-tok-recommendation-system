from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
MOCK_DATA_PATH = ROOT / "data" / "mock" / "tiktok_posts_mock.jsonl"

# TODO: centralize other reusable paths/config (e.g., reports, indices) here.
