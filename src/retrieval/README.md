# Retrieval Module (Skeleton)

- `index.py`: holds an in-memory index and save/load stubs.
- `search.py`: simple TF-IDF placeholder search; swap in BM25/SBERT/FAISS later.
- `scripts/build_index.py` & `scripts/query_index.py`: CLI wrappers for indexing and querying.
